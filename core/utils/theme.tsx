import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    mode: "dark",
    background: {
      default: "#000", // Setting default background color to black
    },
  },
});

export { theme };
